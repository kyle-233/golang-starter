# 数组Array

- 数组是同一种数据类型的固定长度的序列
- 数组定义：var a [len]int，比如var a [5]int，长度不能变
- [5]int [10]int 是不同的数据类型
- 下标访问
- 访问越界，会panic
- 数组是值传递，赋值和传参会复制整个数组
- 支持 == !=，
- 指针数组 [5]*T, 数组指针 *[5]T

### 1.1.1.数组初始化
```

var arr0 [5]int = [5]int{1, 2, 3}
```