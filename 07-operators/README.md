# 运算符
- 算术运算符
- 关系运算符
- 逻辑运算符
- 位运算符
- 赋值运算符

## 算术运算符
+ - * / % （加、减、乘、除、取余）

## 关系运算符
== != > >= < <=（等于、不等于、大于、大于等于、小于、小于等于）

## 逻辑运算符
&& || !（且、或、非）

## 位运算符
& | ^ << >> （与、或、异或、左移、右移）

## 赋值运算符
= += -= *= /= %= <<= >>= &= |= ^=
